name: MkDocs Build

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build MkDocs site
        uses: romw314/mkdocs-action@v9
        with:
          output-dir: site
          workdir: source